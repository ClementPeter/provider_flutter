// library config.globals;
// import "package:provider_flutter/theme.dart";

// //Creating only one instance so we can access Mytheme and trigger the themeMode from any where
// MyThemeProvider currentTheme = MyThemeProvider();